You can add all these scripts to your BootCaT directory (replacing
some of the scripts that are already there).

Add PotaModule.pm to a directory where Perl looks for modules.

Follow the corpus building steps in web_corpus_how_to.txt.
